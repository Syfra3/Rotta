{
  "format": "rotta-quality-policy/v1",
  "id": "default",
  "version": "1",
  "analyzers": [
    {"id": "manual-evidence", "adapter": "manual-evidence", "supported_dimensions": ["code_smells", "duplication", "complexity", "maintainability", "good_practices", "security", "risk", "tests", "static_analysis", "coverage"], "applies_when": "all", "parser": "quality-result/v1"}
  ],
  "dimensions": [
    {"id": "code_smells", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "duplication", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "complexity", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "maintainability", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "good_practices", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "security", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "risk", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "tests", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "static_analysis", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []},
    {"id": "coverage", "analyzer": "manual-evidence", "gate": "required", "applies_when": "all", "severity_scale": ["info", "warning", "error"], "blocking_severity": "error", "thresholds": []}
  ]
}
